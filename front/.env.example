# Public API origin baked into the client bundle at build time.
VITE_API_BASE_URL=http://localhost:8080

# Internal API origin used by the SSR server at runtime.
API_INTERNAL_URL=http://localhost:8080

PORT=3000
